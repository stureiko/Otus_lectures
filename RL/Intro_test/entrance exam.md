# Вступительный тест

Входное тестирование для курса «Reinforcement Learning»

Блок 1. Python и основы программирования

Вопрос 1. Что выведет следующий код?

a = [1, 2, 3]
b = a
b.append(4)
print(a)

A. [1, 2, 3]
B. [1, 2, 3, 4]
C. [4]
D. Ошибка выполнения
E. None

---

Вопрос 2. Какая структура данных в Python обеспечивает доступ к элементу по ключу за среднее O(1)?
A. list
B. tuple
C. dict
D. set
E. str

---

Вопрос 3. Что делает выражение *args в определении функции?
A. Передаёт именованные аргументы в функцию
B. Создаёт список аргументов фиксированной длины
C. Принимает произвольное количество позиционных аргументов
D. Создаёт словарь аргументов
E. Используется только в классах

---

Вопрос 4. Какой метод в Python отвечает за инициализацию объекта класса?
A. __call__
B. __str__
C. __init__
D. __new__
E. __repr__

---

Блок 2. Алгоритмы и структуры данных

Вопрос 5. Какова асимптотика поиска элемента в отсортированном массиве с помощью бинарного поиска?
A. O(1)
B. O(log n)
C. O(n)
D. O(n log n)
E. O(n²)

---

Вопрос 6. Какая структура данных наиболее удобна для реализации очереди (FIFO)?
A. stack
B. deque
C. set
D. dict
E. tuple

---

Вопрос 7. Для чего используется хеш-таблица?
A. Хранение отсортированных элементов
B. Быстрый поиск по ключу
C. Хранение только уникальных значений
D. Для реализации стека
E. Для матричных вычислений

---

Блок 3. Математика

Вопрос 8. Как вычисляется скалярное произведение векторов a = (1,2,3), b = (4,5,6)?
A. 1+2+3+4+5+6
B. 14 + 25 + 3*6
C. 1^2 + 2^2 + 3^2
D. |a|*|b|
E. Определяется только для матриц

---

Вопрос 9. Какова вероятность выпадения «орла» при честной монете?
A. 0
B. 0.25
C. 0.5
D. 1
E. 2

---

Вопрос 10. Как называется матрица, у которой все диагональные элементы равны 1, а остальные — 0?
A. Нулевая
B. Диагональная
C. Единичная
D. Симметричная
E. Треугольная

---

Блок 4. Машинное обучение

Вопрос 11. Какую задачу решает логистическая регрессия?
A. Кластеризация
B. Классификация
C. Регрессия непрерывных значений
D. Ранжирование
E. Снижение размерности

---

Вопрос 12. Для чего нужна функция активации в нейронной сети?
A. Для инициализации весов
B. Для добавления нелинейности в модель
C. Для ускорения обучения
D. Для нормализации входных данных
E. Для подсчёта ошибки

---

Вопрос 13. Что означает overfitting (переобучение)?
A. Модель плохо обучается и не достигает низкой ошибки
B. Модель запоминает обучающую выборку и плохо работает на новых данных
C. Модель обучается очень быстро
D. Ошибка на обучении равна нулю
E. Используется слишком маленький learning rate

---

Вопрос 14. Какой метод используется для разделения выборки на обучающую и тестовую?
A. cross_val_score
B. GridSearchCV
C. train_test_split
D. fit_transform
E. OneHotEncoder

---

Блок 5. PyTorch

Вопрос 15. Какой класс в PyTorch используется для определения модели нейронной сети?
A. torch.autograd
B. torch.tensor
C. torch.nn.Module
D. torch.nn.functional
E. torch.optim

---

Вопрос 16. Как вычислить градиенты в PyTorch?
A. tensor.backward()
B. tensor.detach()
C. optimizer.zero_grad()
D. torch.no_grad()
E. tensor.requires_grad(False)

---

Вопрос 17. Какой оптимизатор является расширением градиентного спуска с адаптивным шагом?
A. SGD
B. Adam
C. RMSProp
D. Momentum
E. Adagrad

---

Вопрос 18. Что делает метод .zero_grad() в PyTorch?
A. Устанавливает веса модели в ноль
B. Обнуляет градиенты перед новым шагом обучения
C. Обнуляет лосс
D. Перезапускает оптимизацию
E. Обнуляет выходные данные модели

---

Блок 6. Работа с данными

Вопрос 19. Как в pandas загрузить CSV-файл?
A. pd.load_csv()
B. pd.import_csv()
C. pd.read_csv()
D. pd.csv_read()
E. pd.open_csv()

---

Вопрос 20. Как в NumPy создать массив из чисел от 0 до 9?
A. np.array(10)
B. np.arange(10)
C. np.linspace(0,10)
D. np.zeros(10)
E. np.ones(10)

---

Такой тест позволяет проверить:

- Python и основы ООП (1–4)
- Алгоритмы и структуры данных (5–7)
- Базовую математику (8–10)
- Основы ML (11–14)
- PyTorch (15–18)
- Pandas / NumPy (19–20)

---

Ключ с ответами

Блок 1. Python и основы программирования
1 → B ([1, 2, 3, 4], потому что b и a указывают на один и тот же список)
2 → C (dict — доступ за O(1) в среднем)
3 → C (*args собирает произвольное количество позиционных аргументов)
4 → C (__init__ — конструктор класса)

---

Блок 2. Алгоритмы и структуры данных
5 → B (бинарный поиск работает за O(log n))
6 → B (deque — оптимальная структура для очереди FIFO)
7 → B (хеш-таблица нужна для быстрого поиска по ключу)

---

Блок 3. Математика
8 → B (скалярное произведение: 14 + 25 + 3*6 = 32)
9 → C (честная монета — вероятность 0.5)
10 → C (единичная матрица)

---

Блок 4. Машинное обучение
11 → B (логистическая регрессия решает задачу классификации)
12 → B (функция активации добавляет нелинейность)
13 → B (переобучение — запоминание обучающей выборки)
14 → C (train_test_split делит данные на train/test)

---

Блок 5. PyTorch
15 → C (torch.nn.Module — базовый класс моделей)
16 → A (tensor.backward() считает градиенты)
17 → B (Adam — оптимизатор с адаптивным шагом)
18 → B (.zero_grad() обнуляет градиенты перед шагом оптимизации)

---

Блок 6. Работа с данными
19 → C (pd.read_csv())
20 → B (np.arange(10))

Шкала оценки

- 0–10 правильных → слабая база, рекомендуется пройти подготовительные материалы (Python, ML basics, PyTorch).
- 11–15 правильных → средний уровень, можно идти на курс, но потребуется подтянуть отдельные темы.
- 16–20 правильных → хорошая подготовка, студент готов к курсу по Reinforcement Learning.
